spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_simple_crud
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      generate-ddl: true
    show-sql: true
  security:
    user:
      name: rudycinho
      password: spring